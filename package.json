{
  "name": "image-effects",
  "version": "1.0.0",
  "main": "index.js",
  "author": "ArmandDu <mail@armanddu.com>",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build:wasm": "yarn workspace image-effects build",
    "build:web": "yarn workspace web build",
    "build": "yarn build:wasm && yarn build:web"
  },
  "devDependencies": {
    "prettier": "^2.5.1"
  }
}
